--- Makefile.orig	2010-06-17 10:00:17.273943872 +0200
+++ Makefile	2010-06-17 10:07:03.763943649 +0200
@@ -6,13 +6,19 @@
 INC_DIR	= ./include
 LIB_DIR	= ./lib
 BIN	= siftfeat match dspfeat
+INSTALL= install
 
-all: $(BIN) libfeat.a docs
+all: $(BIN) libsift.la docs
 
 docs:
 	doxygen Doxyfile
 
-libfeat.a:
+install: 
+	make -C ${SRC_DIR} $@
+	${INSTALL} -d ${PREFIX}/include/sift
+	${INSTALL} -m 644 include/*.h ${PREFIX}/include/sift
+
+libsift.la:
 	make -C $(SRC_DIR) $@
 
 $(BIN):
