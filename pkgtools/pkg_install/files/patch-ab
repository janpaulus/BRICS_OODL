--- lib/Makefile.in~	2009-05-01 19:41:26.000000000 +0200
+++ lib/Makefile.in	2009-11-23 00:10:57.000000000 +0100
@@ -33,16 +33,16 @@
 
 CPPFLAGS+=	-DSYSCONFDIR=\"$(sysconfdir)\"
 
-.if !empty(BOOTSTRAP)
+ifneq (,$(strip $(BOOTSTRAP)))
 CPPFLAGS+=	-DBOOTSTRAP
-.else
+else
 OBJS+=	pkg_io.o pkg_signature.o
-.endif
+endif
 
-.if !empty(SSL_SUPPORT)
+ifneq (,$(strip $(SSL_SUPPORT)))
 CPPFLAGS+=	-DHAVE_SSL
 OBJS+=		pkcs7.o
-.endif
+endif
 
 all: $(LIB)
 
