--- Makefile.in~	2009-11-23 00:08:43.000000000 +0100
+++ Makefile.in	2009-11-23 00:09:58.000000000 +0100
@@ -2,11 +2,11 @@
 
 BOOTSTRAP=	@bootstrap@
 
-.if empty(BOOTSTRAP)
+ifeq (,$(strip $(BOOTSTRAP)))
 SUBDIRS=	lib add admin create delete info
-.else
+else
 SUBDIRS=	lib admin create info
-.endif
+endif
 
 all:
 	@for dir in $(SUBDIRS); do \
